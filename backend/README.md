# College Management Backend Setup

1. Run the queries in `schema.sql` in your Supabase SQL Editor.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
3. Run the server:
   ```bash
   python server.py
   ```
The app will use a fallback mock database if no Supabase environment variables are available.
